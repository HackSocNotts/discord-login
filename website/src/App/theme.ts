import { createMuiTheme } from "@material-ui/core/styles";

const HackSocTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#00953c",
    },
  },
});

export default HackSocTheme;
